apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: solr-data
spec:
  storageClassName: ""
  volumeName: solr-data
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
