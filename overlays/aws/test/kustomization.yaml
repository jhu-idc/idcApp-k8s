apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../../base
  - ../base

resources:
  - namespace.yaml
  - resources/
  - volumes/

patchesStrategicMerge:
  - resources/drupal_service_patch.yaml

namespace: test
