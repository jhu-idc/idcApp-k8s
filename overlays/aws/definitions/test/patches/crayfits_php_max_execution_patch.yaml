apiVersion: apps/v1
kind: Deployment
metadata:
  name: crayfits
spec:
  template:
    spec:
      containers:
        - name: crayfits
          env:
            - name: PHP_MAX_EXECUTION_TIME
              value: "7200"