apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - activemq_secrets.yaml
  - drupal_secrets.yaml
  - drupal-sealed.yaml
  - saml-secrets-sealed.yaml
  - reverseproxy-configmap.yaml
  